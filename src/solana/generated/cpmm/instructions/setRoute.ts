/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { CPMM_PROGRAM_ADDRESS } from '../programs';

export const SET_ROUTE_DISCRIMINATOR = new Uint8Array([
  244, 231, 3, 84, 233, 61, 146, 149,
]);

export function getSetRouteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_ROUTE_DISCRIMINATOR);
}

export type SetRouteInstruction<
  TProgram extends string = typeof CPMM_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountNextPool extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountNextPool extends string
        ? ReadonlyAccount<TAccountNextPool>
        : TAccountNextPool,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type SetRouteInstructionData = {
  discriminator: ReadonlyUint8Array;
  routeNextPool: Address;
  routeBridgeMint: Address;
};

export type SetRouteInstructionDataArgs = {
  routeNextPool: Address;
  routeBridgeMint: Address;
};

export function getSetRouteInstructionDataEncoder(): FixedSizeEncoder<SetRouteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['routeNextPool', getAddressEncoder()],
      ['routeBridgeMint', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ROUTE_DISCRIMINATOR })
  );
}

export function getSetRouteInstructionDataDecoder(): FixedSizeDecoder<SetRouteInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['routeNextPool', getAddressDecoder()],
    ['routeBridgeMint', getAddressDecoder()],
  ]);
}

export function getSetRouteInstructionDataCodec(): FixedSizeCodec<
  SetRouteInstructionDataArgs,
  SetRouteInstructionData
> {
  return combineCodec(
    getSetRouteInstructionDataEncoder(),
    getSetRouteInstructionDataDecoder()
  );
}

export type SetRouteInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountNextPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  nextPool?: Address<TAccountNextPool>;
  admin: TransactionSigner<TAccountAdmin>;
  routeNextPool: SetRouteInstructionDataArgs['routeNextPool'];
  routeBridgeMint: SetRouteInstructionDataArgs['routeBridgeMint'];
};

export function getSetRouteInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountNextPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof CPMM_PROGRAM_ADDRESS,
>(
  input: SetRouteInput<
    TAccountConfig,
    TAccountPool,
    TAccountNextPool,
    TAccountAdmin
  >,
  config?: { programAddress?: TProgramAddress }
): SetRouteInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountNextPool,
  TAccountAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? CPMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    nextPool: { value: input.nextPool ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('config', accounts.config),
      getAccountMeta('pool', accounts.pool),
      getAccountMeta('nextPool', accounts.nextPool),
      getAccountMeta('admin', accounts.admin),
    ],
    data: getSetRouteInstructionDataEncoder().encode(
      args as SetRouteInstructionDataArgs
    ),
    programAddress,
  } as SetRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountNextPool,
    TAccountAdmin
  >);
}

export type ParsedSetRouteInstruction<
  TProgram extends string = typeof CPMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    nextPool?: TAccountMetas[2] | undefined;
    admin: TAccountMetas[3];
  };
  data: SetRouteInstructionData;
};

export function parseSetRouteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetRouteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === CPMM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      nextPool: getNextOptionalAccount(),
      admin: getNextAccount(),
    },
    data: getSetRouteInstructionDataDecoder().decode(instruction.data),
  };
}
